/// Stub for Phase 5 — Before/After split-view comparison widget.
///
/// Will be a `GtkDrawingArea`-based widget with Cairo clipping and
/// a draggable vertical divider (GtkGestureDrag).
pub struct BeforeAfterViewer;
